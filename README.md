# snake
snake

install dependents by
```shell
pip install tkinter
pip install pygame
pip install time
pip install random
```
*What?You didn't have Pip?*

run it by
```shell
pyt main.py
```
*What?You didn't have Python?*
